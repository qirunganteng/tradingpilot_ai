package com.tradepilot.feature.screenshot

import androidx.compose.foundation.layout.Column
import androidx.compose.foundation.layout.fillMaxSize
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier

/**
 * Capture, crop, compress screenshot chart
 * Diimplementasikan penuh pada Fase 2 (lihat Blueprint bagian 14: Roadmap).
 */
@Composable
fun CaptureScreen(modifier: Modifier = Modifier) {
    Column(modifier = modifier.fillMaxSize()) {
        Text(text = "TODO Fase 2: Capture, crop, compress screenshot chart")
    }
}
