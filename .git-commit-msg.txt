feat(worker): Fase 6 - endpoint OCR & Risk Engine di backend

- Tambah POST /api/v1/ocr: baca ulang label/angka di chart via Gemini
  vision (prompt sempit, tanpa kesimpulan trading). Pakai rate limit
  yang sama dengan /analyze karena sama-sama panggil Gemini.
- Tambah POST /api/v1/calculate-risk: port persis dari
  CalculateRiskUseCase.kt (:shared) ke TypeScript, supaya kalkulasi
  lot/RR punya satu sumber kebenaran di server (bukan cuma kode yang
  sama di-compile ulang di tiap client).
- Tambah unit test riskEngine.test.ts (parity dengan use case Kotlin,
  custom pip size/value untuk XAUUSD, div-by-zero guard, validasi).
- Tipe baru: OcrLabel, OcrResult, OcrRequestBody,
  CalculateRiskRequestBody, RiskRecommendation.

Catatan: typecheck (tsc --noEmit) lolos bersih. vitest belum bisa
dijalankan di sandbox ini (workerd butuh stdio interaktif yang tidak
tersedia) - jalankan `npm test` di terminal biasa sebelum deploy.
Endpoint OCR belum pernah dites end-to-end ke Gemini API sungguhan.
